name: short_video_mcp
description: MCP stdio server for new_short_video project tooling
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # 无第三方依赖，仅用 dart:io / dart:convert
