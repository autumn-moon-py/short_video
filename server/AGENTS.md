# AGENTS.md instructions for D:\project\flutter\new_short_video\server

## 基础规范
1. 请保持对话语言为中文。
2. 用户系统为 Windows，所有命令与排查步骤优先按 Windows 环境思考。
3. 代码注释习惯为适量核心注释，不需要大量注释。
4. 禁止在任何地方使用 emoji。

## 编码识别规范
1. 不得把“终端显示异常”直接判断为“文件乱码”。
2. 读取文本文件时，默认先假设文件内容正常，再判断是否存在编码问题，不能先入为主把正常中文、日文或注释当成乱码。
3. 发现疑似乱码时，必须区分以下几类问题：
   - 文件实际编码异常；
   - PowerShell/终端代码页导致的显示异常；
   - 工具输出编码与终端解码不一致；
   - 字体或复制链路导致的观感异常。
4. 未经验证，不得声称“该文件是乱码”或“该文件编码损坏”。
5. 如果同一内容在不同读取方式下结果不一致，应优先判断为“读取链路或显示链路问题”，不是立即认定源文件损坏。

## RUN / Shell 指令规范
1. 在 Windows 下读取文件内容时，RUN 指令必须优先使用明确编码或可验证编码的方式，避免让终端默认编码误导判断。
2. 使用 `Get-Content`、`type`、`rg`、`git diff`、`git show` 等命令查看文本时，要意识到输出乱码可能只是控制台解码问题，不等于文件本身乱码。
3. 当输出中出现中文、日文或其他非 ASCII 文本异常时，必须先做二次验证，再下结论。可选验证方式包括：
   - 改用另一种读取方式再次查看；
   - 结合十六进制、字节级或已知编码方式确认；
   - 对比编辑器内显示结果与命令行结果是否一致。
4. 对于 `rg` 或其他搜索命令命中的文本，不得仅凭命中结果中的异常字符就判定源文件乱码；必要时必须回到文件内容本身再次确认。
5. 如果只是命令输出层乱码，应明确表述为“终端输出疑似编码不匹配”或“当前读取方式显示异常”，不能表述成“文件乱码”。

## 行为要求
1. 遇到编码问题时，先验证，再描述结论，再决定是否修复。
2. 结论表述必须精确，区分“文件编码异常”“终端显示异常”“命令输出异常”“暂未确认”。
3. 若证据不足，明确写“未确认”，不要擅自扩大判断。
